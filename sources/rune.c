#include <stdio.h>
#include <stdlib.h>

int main( int argc, char **argv[] ) {
  int c;
  FILE *src;
  
  if ( argc > 2 ) {
    fprintf( "Usage:  rune < foo.rune >\n" );
  }
  
  fopen( src );
  free ( /* tape or cell */ );
  return 0;
}
